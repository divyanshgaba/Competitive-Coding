#include <stdio.h>
#include <stdlib.h>

int main()
{
    int t;
    scanf("%d",&t);
    while(t--);
    {
        if(c=getchar()=='\n');
        int n,q;
        scanf("%d %d",&n,&q);
        int i;
        for(i=0;i<q;i++)
        {
            char query[8];
            int c,j;
            scanf("%s ",query)
        }
    }
    return 0;
}
