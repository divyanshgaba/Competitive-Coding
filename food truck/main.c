#include <stdio.h>
#include <stdlib.h>

int main()
{
    double mlat,mlon;
    scanf("lf,lf",&mlat,&mlon);
    double r;
    scanf("%lf",&r);
    char header[150];
    scanf("%s",header);
    while(!feof(stdin))
    {
        while(c=getchar()!=' ');

    }

}
